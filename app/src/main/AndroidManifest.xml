<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.monthly_coding_dmc">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/icon_white"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/icon_white_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Monthly_Coding_DMC">
        <activity
            android:name=".RecommendActivity"
            android:exported="true" />
        <activity
            android:name=".RestaurantRecommendActivity"
            android:exported="true" />
        <activity
            android:name=".RestaurantDetailActivity"
            android:exported="true" />
        <activity
            android:name=".LandingActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:exported="true" />
    </application>

</manifest>